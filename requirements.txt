streamlit>=1.28,<1.40
# Scientific stack pinned to the corrected-revision freeze (REQUIREMENTS_FROZEN.md).
# Loose rdkit>=2022.9.1 allowed Cloud to install a different RDKit than the freeze,
# shifting ECFP4/PhysChem for some molecules (raw ensemble drift; cal often still matched
# via isotonic plateaus).
numpy>=2.0.0,<2.3.0
pandas>=2.0.0,<2.3.0
scikit-learn>=1.3.0,<1.8.0
lightgbm==4.5.0
rdkit==2025.03.3
joblib>=1.3.0,<1.6.0
